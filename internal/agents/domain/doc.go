// Package domain is the domain layer of the agents module, which owns
// the agent runtime: turns, tool calls, approvals and budget.
//
// domain holds the pure types and rules of the agents module.
// It imports nothing outside the standard library and other modules' domain (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package domain
