// Package ports is the ports layer of the agents module, which owns
// the agent runtime: turns, tool calls, approvals and budget.
//
// ports declares the interfaces the agents module publishes to the rest of the daemon.
// It may import its own domain and other modules' domain (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package ports
