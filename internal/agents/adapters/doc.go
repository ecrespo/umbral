// Package adapters is the adapters layer of the agents module, which owns
// the agent runtime: turns, tool calls, approvals and budget.
//
// adapters implements the agents ports against the outside world.
// It may import its own ports, its own domain and external libraries (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package adapters
