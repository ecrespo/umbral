// Package domain is the domain layer of the tools module, which owns
// the tool registry and the built-in file, search and network tools.
//
// domain holds the pure types and rules of the tools module.
// It imports nothing outside the standard library and other modules' domain (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package domain
