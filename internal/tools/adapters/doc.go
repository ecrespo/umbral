// Package adapters is the adapters layer of the tools module, which owns
// the tool registry and the built-in file, search and network tools.
//
// adapters implements the tools ports against the outside world.
// It may import its own ports, its own domain and external libraries (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package adapters
