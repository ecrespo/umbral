// Package store owns the SQLite database described in `specs/data-model/umbral-schema.md`:
// connection pragmas, forward-only migrations and restart recovery.
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package store
