// Package adapters is the adapters layer of the context module, which owns
// context assembly: rules files, attachments and git state.
//
// adapters implements the context ports against the outside world.
// It may import its own ports, its own domain and external libraries (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package adapters
