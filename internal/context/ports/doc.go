// Package ports is the ports layer of the context module, which owns
// context assembly: rules files, attachments and git state.
//
// ports declares the interfaces the context module publishes to the rest of the daemon.
// It may import its own domain and other modules' domain (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package ports
