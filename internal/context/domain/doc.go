// Package domain is the domain layer of the context module, which owns
// context assembly: rules files, attachments and git state.
//
// domain holds the pure types and rules of the context module.
// It imports nothing outside the standard library and other modules' domain (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package domain
