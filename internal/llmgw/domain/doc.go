// Package domain is the domain layer of the llmgw module, which owns
// the model gateway: provider catalogue, routing, fallback and usage accounting.
//
// domain holds the pure types and rules of the llmgw module.
// It imports nothing outside the standard library and other modules' domain (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package domain
