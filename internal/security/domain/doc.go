// Package domain is the domain layer of the security module, which owns
// the policy engine, secret redaction and the egress audit trail.
//
// domain holds the pure types and rules of the security module.
// It imports nothing outside the standard library and other modules' domain (Art. 3).
//
// Scaffolding for T-F0-01; the implementation arrives with its own task.
package domain
