# CHECKPOINT — specs 1.2, findings closed and gate green

> 2026-09-20 · 129 files (excluding `tools/node_modules`) · all artifacts in English

| Artifact | Path | Exists |
|---|---|---|
| Constitution 1.1 (2 amendments) | `specs/constitution.md` | ✅ |
| PRD 1.2 | `specs/prd/umbral-mvp.md` | ✅ |
| API Spec 1.2 | `specs/api/umbral-daemon-api-v1.md` | ✅ |
| Tech Design 1.2 | `specs/technical/umbral-architecture.md` | ✅ |
| Data Model 1.2 | `specs/data-model/umbral-schema.md` | ✅ |
| Plan 1.2 | `specs/plans/umbral-mvp-plan.md` | ✅ |
| Tasks F0 | `specs/tasks/umbral-f0-tasks.md` | ✅ |
| Tasks F1 | `specs/tasks/umbral-f1-tasks.md` | ✅ |
| Tasks hardening | `specs/tasks/umbral-hardening-tasks.md` | ✅ |
| Analyze closing (current) | `specs/analyze/analyze-2026-09-20b.md` | ✅ |
| Decision record (folded) | `changes/_archive/2026-09-close-findings/proposal.md` | ✅ |
| ADR-0001 | `docs/adr/ADR-0001-architectural-style.md` | ✅ |
| ADR-0002 | `docs/adr/ADR-0002-orchestration-surface.md` | ✅ |
| Architecture v0.2 | `docs/ARCHITECTURE.md` | ✅ |
| Diagram (Excalidraw) | `docs/diagrams/umbral-architecture.excalidraw` | ✅ |

| Check | Result |
|---|---|
| `tools/sdd_check.py` | **exit 0** · REQs defined: 112 (MUST 106) · with task: 106/106 · DDL OK (26 tables/objects) · migration 0001 in isolation OK |
| `tools/mermaid_check.mjs` | mermaid: 11/11 diagrams valid — *see the correction below: 15 diagrams exist once both lineages are merged* |
| Icon kit checksums | OK |
| Bootstrap dry run | 58 issues |
| Migration 0001 in isolation | OK — A-01 closed |
| FTS triggers after UPDATE | index refreshed, no stale rows |
| `client_msg_id` uniqueness · single active bundle | both enforced by the DDL |
| Open deltas | none; `changes/` holds only `_archive/` |
| Relative links · Spanish scan | 0 broken · 0 hits outside known false positives |

## Decisions folded

- **Signing (B-01):** remote rule updates require a verified Ed25519 signature; key lifecycle in the product; fail closed after three failures or with no key; recovery with `rules.rollback` and `rules.reset`, both offline and without a key.
- **Wait monitoring (B-02):** limits degrade the operation and never the connection; inventory with age and stall flag; cancellation that does not touch the observed turn; stall detection that notifies without killing.
- **Environment secrets (A-03):** `env:<VAR>` accepted when the keyring is unavailable and the fallback is enabled, reported as degraded. Art. 5 amended.
- **A-08, A-09, A-11, A-12, B-03…B-10:** folded as recommended.
- **A-01, A-02, A-04, A-05, A-06, A-07, A-13, A-15:** mechanical fixes folded; the three deltas are archived.

## Honest status

- The gate now exits 0. The only remaining message is the LOW about T-F0-01, which is the accepted convention for infrastructure tasks.
- Open in the closing Analyze: C-01 (SQLite write failure under persist-first) and C-02 (no documented custody for the signing key). Neither blocks T-F0-01; they block T-F1-13 and T-F1-30.
- C-02 needs a decision from you that is organizational rather than technical: where the private key lives and who can sign.
- The estimate is now 13-16 weeks. Still an estimate.
- Nothing is implemented: the first code is still T-F0-01.

> **Correction, 2026-09-20 (reconciliation):** that last line was wrong, and so was the
> version table above it. This checkpoint was taken against a working copy restored from
> `umbral-repo-en.zip`, whose baseline predates the F0 implementation commits of 2026-09-11.
> At the time it was written, T-F0-01 … T-F0-10 and T-PKG-01/02 were already committed, and
> the base specs stood at API 1.4 / Data Model 1.3 / PRD 1.3 / Tech 1.3 rather than 1.2.
> See `2026-09-20-reconciliation.md`.
