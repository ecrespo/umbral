# CHECKPOINT — T-F0-01, scaffolding and quality gate (verified against the filesystem)

> 2026-09-11 · branch `feat/T-F0-01-scaffolding` · first product code in the repository.

## What exists now

| Artifact | Path | Purpose |
|---|---|---|
| Go module | `go.mod` | `github.com/ecrespo/umbral`, Go 1.27 |
| Daemon | `cmd/umbrald/main.go` | composition root; `-version` works, the listener arrives in T-F0-03 |
| CLI | `cmd/umb/main.go` | exits 69 (`EX_UNAVAILABLE`), the contract REQ-CLI-003 reserves |
| TUI | `cmd/umbral-tui/main.go` | skeleton until T-F0-12 |
| Module skeleton | `internal/**/doc.go` | 24 documented packages following Tech Design §5.1 |
| Task runner | `Taskfile.yml` | `lint`, `arch`, `arch:selftest`, `test`, `test:cover`, `bench`, `specs`, `build`, `ci`, `tools:install` |
| Lint gate | `.golangci.yml` | standard set plus gosec, revive, errorlint, nolintlint and fifteen more; gofumpt as the formatter |
| Boundaries | `.go-arch-lint.yml` | Tech Design §5.2 written out component by component |
| Boundary self-test | `scripts/arch_selftest.sh` | injects `sessions` → `agents`, asserts rejection, removes it |
| Hooks | `.pre-commit-config.yaml` | gitleaks plus the Go tools as system hooks |
| Pipeline | `.github/workflows/ci.yml` | four jobs: specs, lint, arch, test on Linux and macOS |

## Verifications run locally

| Gate | Command | Result |
|---|---|---|
| Art. 9 | `python3 tools/sdd_check.py` | exit 0 · 73 REQs · 67/67 MUST covered · 45 tasks |
| Art. 9 | `node tools/mermaid_check.mjs` | 14/14 valid |
| Art. 1 | `task lint` | gofumpt clean, `go vet` clean, golangci-lint 0 issues, govulncheck no vulnerabilities, gitleaks no leaks |
| Art. 3 | `task arch` | no warnings |
| Art. 3 | `task arch:selftest` | the forbidden import is rejected, exit 1 as required |
| Art. 2 | `task test` (`go test -race ./...`) | green |
| build | `task build` | three binaries; `umbrald -version` and `umb` behave as specified |

## Honest status

- **Go was installed into `~/.local/go` (1.27.1), not system-wide**, because the machine had no Go and installing one needs root. Add `export PATH="$HOME/.local/go/bin:$HOME/go/bin:$PATH"` to your shell rc, or install Go with your package manager and delete that directory.
- **Zig is still missing.** T-F0-04 and T-F0-05 need it to build libghostty-vt. That is the next prerequisite to close.
- `gosec` rule G115 is excluded, with the reason written in `.golangci.yml`: it fires on every epoch-ms and micro-USD conversion Art. 6 mandates. It is re-enabled when the store layer lands and the conversions can be audited individually.
- The pipeline has **never run on real GitHub**. It is syntactically valid YAML and every step mirrors a command that passes locally, nothing more.
- `internal/**` holds documentation packages only. There is no behaviour there yet; the first real code is T-F0-02.
- The hooks in `.pre-commit-config.yaml` have not been executed: `pre-commit` is not installed on this machine.
