# CHECKPOINT — Phase 0: folding the analyze-fixes delta (verified against the filesystem)

> 2026-09-11 · scope: `specs/`, `changes/`, `tools/`, `AGENTS.md`, `CHANGELOG.md` · no product code.

## What was executed

| Task | State | Where it landed |
|---|---|---|
| T-FIX-01 (A-01, A-07) | done | `specs/data-model/umbral-schema.md` §2.4, §2.5, §3, §5.1 · `tools/sdd_check.py` · `specs/tasks/umbral-f0-tasks.md` T-F0-02 · `specs/tasks/umbral-f1-tasks.md` T-F1-01 |
| T-FIX-02 (A-02) | done | `specs/technical/umbral-architecture.md` §8 and new §8.1 · T-F0-07 |
| T-FIX-03 (A-03, A-04) | done | `specs/prd/umbral-mvp.md` §6.3, §6.6 · API §5.14 · Data Model §2.6 · T-F1-02, T-F1-13 and the F1 matrix |
| T-FIX-04 (A-05, A-06) | done | API §4 `Session`, §5.21 `mcp.server.add` |
| T-FIX-05 | done | `specs/analyze/analyze-2026-09-11b.md` · delta moved to `changes/_archive/2026-09-analyze-fixes/` |
| A-13 (out of the original delta scope) | done | T-F0-09 file globs narrowed so they no longer overlap T-F0-07 |

## Verifications run

| Verification | Command | Result |
|---|---|---|
| SDD coverage and DDL | `python3 tools/sdd_check.py` | **exit 0** · 73 REQs (67 MUST) · 67/67 with a task and a matrix row · 45 tasks · 0 uncited · migration 0001 in isolation OK (INSERT + FTS MATCH) |
| Migration 0001, independent run | `sqlite3` CLI over the DDL extracted from the Data Model | insert, update and delete keep `blocks_fts` in sync; no stale hit after an update |
| Diagrams | `node tools/mermaid_check.mjs` | 14/14 valid |

## Honest status

- No Go code exists yet. F0 starts at T-F0-01.
- **Go is not installed on this machine** (`go: command not found`), and neither is Zig. T-F0-01 cannot run until the toolchain in the plan's prerequisites is installed.
- `.github/` and `.claude/` do **not** exist in the working tree, even though `docs/checkpoints/2026-09-11-repo.md` lists them. That checkpoint describes a package that was never committed here; the repo has a single commit, `b5971da`.
- Every artifact is still `DRAFT`: the second-pass Analyze clears the CRITICAL, it does not approve the specs. Approval is human.
- The visual identity delta is still unfolded, and its Spanish duplicate `changes/2026-09-identidad-icono/` is still present (finding A-16).
- Nothing has been committed: all the work above is in the working tree.
