# Analyze — Umbral MVP (F0 + F1) · 2026-09-11 (second pass)

> Read-only. Supersedes `specs/analyze/analyze-2026-09-11.md`, which stays as the audit trail.
> Trigger: folding of `changes/_archive/2026-09-analyze-fixes/` (T-FIX-01 … T-FIX-05).
> Artifacts reviewed:
> `specs/constitution.md` · `specs/prd/umbral-mvp.md` · `specs/api/umbral-daemon-api-v1.md` ·
> `specs/technical/umbral-architecture.md` · `specs/data-model/umbral-schema.md` ·
> `specs/plans/umbral-mvp-plan.md` · `specs/tasks/umbral-f0-tasks.md` · `specs/tasks/umbral-f1-tasks.md` ·
> `changes/2026-09-visual-identity/*` and `changes/2026-09-identidad-icono/*` (deltas in review, not folded).

## Automated checks

| Check | Result |
|---|---|
| `python3 tools/sdd_check.py` | exit **0**. 73 REQs defined (67 MUST); **67/67 MUST with a task and a matrix row**; 45 tasks; 0 uncited tasks; 1 infrastructure task citing a constitution article (T-F0-01, Art. 1 and 3, documented exception) |
| Full Data Model DDL in in-memory SQLite | OK (17 tables/objects) |
| Migration 0001 in isolation with `foreign_keys=ON` | **OK** — INSERT into `sessions` and `blocks` accepted, and the FTS `MATCH` returns the new block |
| Migration 0001 with the `sqlite3` CLI (independent run) | OK — insert, update (no stale hit) and delete all keep `blocks_fts` in sync |
| `node tools/mermaid_check.mjs` | 14/14 diagrams valid |

## State of the first-pass findings

| # | Severity | State | Evidence |
|---|---|---|---|
| A-01 | CRITICAL | **RESOLVED** | `threads` is created in migration 0001 (Data Model §5.1, note in §2.5); the checker simulates 0001 with `threads` and asserts an INSERT plus an FTS `MATCH`; `sqlite3` confirms it |
| A-02 | HIGH | **RESOLVED** | Tech Design §8.1 enumerates VT-01 … VT-20 (MUST) and VT-21/VT-22 (SHOULD); T-F0-07 implements that list and fails on a missing `.golden` |
| A-03 | HIGH | **RESOLVED** | REQ-SEC-008 (PRD §6.6) with degraded start; T-F1-02 covers it with `TestKeyringUnavailableDisablesProviders_REQ_SEC_008`. `env:VAR` was **not** accepted as a secret source, so Art. 5 needs no amendment |
| A-04 | HIGH | **RESOLVED** | Optional `client_msg_id` in `thread.send` (API §5.14), column plus partial unique index in `messages` (Data Model §2.6), REQ-AGT-015 and `TestSendIdempotentByClientMsgID_REQ_AGT_015` in T-F1-13 |
| A-05 | MEDIUM | **RESOLVED** | `owner_thread_id` added to the API `Session` schema; `protocol_version` stays at 1 (additive) |
| A-06 | MEDIUM | **RESOLVED** | `mcp.server.add` takes `env_refs`; the column stays `env_refs_json`. The old name survives only in the archived delta and in the first-pass report |
| A-07 | MEDIUM | **RESOLVED** | The three `blocks_fts_ai/ad/au` triggers are written out in Data Model §2.4 and belong to migration 0001 |
| A-13 | LOW | **RESOLVED** (folded ahead of schedule) | T-F0-09 now declares `adapters/shellinteg/**` and `adapters/store/**`, so it no longer overlaps the T-F0-07 [P] globs |

## Findings still open

| # | Severity | Category | Finding | Suggested moment |
|---|---|---|---|---|
| A-08 | MEDIUM | Ambiguity | Performance limits (REQ-TERM-001, TERM-006, BLK-006) do not set a reference machine | before T-F0-13, which is the task that turns them into a CI gate |
| A-09 | MEDIUM | Ambiguity | REQ-SEC-001 has no entropy threshold or minimum length; REQ-CTX-001 does not say whether precedence is by file name or directory depth | before T-F1-04 and T-F1-11 |
| A-10 | MEDIUM | Unhappy paths | No criterion for a SQLite write failure under the "persist before notifying" rule (DD-007) | before T-F1-13 |
| A-11 | MEDIUM | Unhappy paths | `fetch_url` has no size limit, timeout or allowed schemes (SSRF) | before T-F1-09 |
| A-12 | MEDIUM | Delta | REQ-PKG-004/005/007 are MUST but belong to F2; folding the visual-identity delta as-is would put unclosable MUSTs in the MVP | when the visual-identity delta is folded |
| A-14 | LOW | Terminology | "workspace" is used in the PRD but defined only in Tech Design §5.3 | any time |
| A-15 | LOW | Delta | The icon kit's `CHECKSUMS.sha256` covers the `.desktop` file that T-PKG-02 will modify | inside T-PKG-02 |
| A-16 | LOW | Duplication | `changes/2026-09-identidad-icono/` is the superseded Spanish copy of `changes/2026-09-visual-identity/`. It defines the same REQ-PKG IDs and the same T-PKG task IDs twice, and it contradicts the "everything in English" rule in `AGENTS.md` | before folding the visual identity |

None of the open findings blocks F0: A-08 lands in T-F0-13 and every other one is F1 or packaging.

## Constitution

No violations.

- Art. 6: `client_msg_id` is a ULID; no change to the time or money conventions.
- Art. 8: the API change is purely additive, so `protocol_version` stays at 1.
- Art. 9: the change entered as a Delta and `specs/` was updated as part of the Definition of Done.

## Verdict

**READY TO IMPLEMENT (F0).** No CRITICAL and no HIGH findings remain. The plan's prerequisite
"Analyze without CRITICAL findings" is met, so T-F0-01 can start. The open MEDIUM findings are
attached above to the task that first depends on each of them.

> The Analyze approves nothing: it presents findings. Approval is human.
