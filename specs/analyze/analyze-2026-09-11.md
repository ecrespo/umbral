# Analyze — Umbral MVP (F0 + F1) · 2026-09-11

> Read-only. Artifacts reviewed:
> `specs/constitution.md` · `specs/prd/umbral-mvp.md` · `specs/api/umbral-daemon-api-v1.md` ·
> `specs/technical/umbral-architecture.md` · `specs/data-model/umbral-schema.md` ·
> `specs/plans/umbral-mvp-plan.md` · `specs/tasks/umbral-f0-tasks.md` · `specs/tasks/umbral-f1-tasks.md` ·
> `changes/2026-09-visual-identity/*` (delta in review, not yet folded).

## Automated checks

| Check | Result |
|---|---|
| `python3 tools/sdd_check.py` | 71 REQs defined (65 MUST); **65/65 MUST with a task**; 40 tasks; 1 infrastructure task without a functional REQ (T-F0-01, cites Art. 1 and 3; accepted by convention); 0 ghost REQs |
| Data Model DDL executed in in-memory SQLite (all tables together) | OK (17 objects) |
| Migration 0001 DDL in isolation + INSERT with `foreign_keys=ON` (`sdd_check.py`) | **FAILS**, see A-01 (the checker exits with code 1) |
| Mermaid (9 diagrams in 4 files) | 9/9 valid (`mermaid.parse`) |

## Findings

| # | Severity | Category | Finding | Artifacts | Suggestion |
|---|---|---|---|---|---|
| A-01 | **CRITICAL** | Data | `sessions.owner_thread_id` and `blocks.thread_id` declare `REFERENCES threads(id)`, but `threads` is created in migration 0002 (F1). With `foreign_keys=ON`, SQLite rejects **every** INSERT into `sessions` and `blocks` (even with NULL) with `no such table: main.threads`, verified. F0 would not work. | Data Model §2.1-2.2, T-F0-02, T-F1-01 | Create `threads` in 0001 (minimal table) **or** add the FK columns in 0002 by rebuilding the tables. The first option is cheaper. |
| A-02 | HIGH | Ambiguity | REQ-TERM-002 requires "100 % of the MUST cases of the conformance suite", but no artifact enumerates those cases. An agent would invent them in T-F0-07. | PRD, Tech Design §8, T-F0-07 | Appendix in the Tech Design with the closed list of MUST cases (≈ 20) and their sources (vttest, esctest). |
| A-03 | HIGH | Unhappy paths | No criterion for an unavailable keyring (headless Linux / SSH session without Secret Service). The daemon could not load keys and its behavior is undefined. | PRD §6.6, T-F1-02 | New IF…THEN REQ: start with remote providers disabled, `model.health = down` with a reason and a notice in `umb status`. Decide whether `env:VAR` is allowed as an alternative source (requires amending Art. 5). |
| A-04 | HIGH | Unhappy paths | `thread.send` has no idempotency key. A client retry after a disconnect would duplicate the turn, and with it the executed commands. | API §5.14, Data Model §2.6 | Add an optional `client_msg_id` (ULID) unique per `(thread_id, client_msg_id)`; a duplicate returns the original `turn_id`. |
| A-05 | MEDIUM | Coverage | The API `Session` schema lacks `owner_thread_id`, which exists in the Data Model and which the TUI needs to tell agent PTYs apart. | API §4, Data Model §2.1 | Add the optional field to the schema (additive change, no `protocol_version` bump). |
| A-06 | MEDIUM | Terminology | `mcp.server.add` uses `env_keyring_refs`; the Data Model uses `env_refs_json`. | API §5.21, Data Model §2.13 | Unify as `env_refs` (API) / `env_refs_json` (column). |
| A-07 | MEDIUM | Data | The `blocks_fts` sync triggers appear only as a comment; REQ-BLK-006 depends on them. | Data Model §2.4, T-F0-02 | Write the three triggers in the spec. |
| A-08 | MEDIUM | Ambiguity | Performance limits (REQ-TERM-001, TERM-006, BLK-006) do not set a reference hardware. | PRD §7, T-F0-13 | Set the reference: CI runner, plus the ThinkPad P14s Gen 6 as the manual validation machine. |
| A-09 | MEDIUM | Ambiguity | REQ-SEC-001 sets no entropy threshold or minimum length; REQ-CTX-001 does not state whether "order of precedence" means by file name or by directory depth. | PRD §6.6, §6.4 | Specify: entropy ≥ 4.5 bits/char and length ≥ 20; precedence by depth (closest to the cwd wins) and then by name. |
| A-10 | MEDIUM | Unhappy paths | No criterion for a SQLite write failure (disk full, `SQLITE_BUSY` after `busy_timeout`) under the "persist before notifying" rule (DD-007). | Tech Design DD-007, REQ-AGT-011 | IF…THEN REQ: stop the turn with `stop_reason = storage_error` without running the tool. |
| A-11 | MEDIUM | Unhappy paths | `fetch_url` has no size limit, timeout or allowed schemes. | PRD REQ-AGT-002, T-F1-09 | Set: http/https, 10 s, 2 MiB, no redirects to loopback or private IPs (SSRF). |
| A-12 | MEDIUM | Delta | REQ-PKG-004, 005 and 007 are MUST but belong to F2 (desktop client), outside the MVP PRD scope. Folded as-is, the MVP would have MUSTs impossible to close. | `changes/2026-09-visual-identity/delta-spec.md` | When folding: keep REQ-PKG-001/002/003/006 in the MVP PRD and move 004/005/007 to the F2 PRD. |
| A-13 | LOW | Executability | T-F0-07 [P] and T-F0-09 declare overlapping file globs (`internal/sessions/adapters/**`). | tasks F0 | Narrow T-F0-09 to `adapters/shellinteg/**` and `domain/block*.go`. |
| A-14 | LOW | Terminology | "workspace" is used in the PRD (REQ-AGT-013) but only defined in Tech Design §5.3. | PRD, Tech Design | Add the definition to the PRD glossary. |
| A-15 | LOW | Delta | The kit's `CHECKSUMS.sha256` includes the `.desktop` file, which T-PKG-02 will modify. The `icons` job will fail after that task unless the checksums are regenerated. | kit, delta tasks | Regenerate `CHECKSUMS.sha256` as part of T-PKG-02. |

## Constitution

No violations.

- Excluding Windows from the MVP matches the stack constraints.
- `auto-edit` mode (REQ-AGT-013) does not violate Art. 5: the `allow` decision is issued by the policy engine according to the mode the user chose.
- A-03 could require an amendment if `env:VAR` is accepted as a secret source.

## Verdict

**FIX CRITICALS FIRST.** A-01 blocks F0. A-02, A-03 and A-04 are gaps an agent would fill by
guessing; closing them before the first batch (T-F0-01…T-F0-04) is advisable. A-02 affects T-F0-07,
A-04 affects F1 and A-03 affects F1, so the first batch can start as soon as A-01 is fixed.

> The Analyze approves nothing: it presents findings. Approval is human.
