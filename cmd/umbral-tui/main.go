// Command umbral-tui is the Bubble Tea v2 terminal client: tabs, splits, block
// navigation and, from F1, the agent panel.
//
// T-F0-01 leaves it as a runnable skeleton; the client arrives in T-F0-12.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "umbral-tui is not implemented yet (T-F0-12)")
	os.Exit(1)
}
